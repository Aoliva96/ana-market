{{! TODO: Change all instances of 'project' to 'item' (keep same pluralization & capitalization) }}
{{! TODO: Contextually change 'needed-funding' to 'item-price' }}
{{! TODO: Item previews should only show an image, the item name, and price. Users must login to view item details }}

<!-- Loop over each object in the projects array -->
{{#each projects as |project|}}
  <div class='row mb-4 project'>
    <div class='col-md-5'>
      <h2>
        <!-- Call helper function with triple brackets to render the emoji span as HTML -->
        {{{get_emoji}}}
        <a href='/project/{{project.id}}'>{{project.name}}</a>
      </h2>
      <p>
        <span for='img' aria-label='money'>💲</span>
        <!-- Pass needed_funding value to the helper function -->
        <span class='dollar-amount'>{{format_amount
            project.needed_funding
          }}</span>
      </p>
      <p>Listed by
        {{project.user.name}}
        on
        {{format_date project.date_created}}</p>
    </div>
    <div class='col-md-7'>
      <p>
        {{project.description}}
      </p>
    </div>
  </div>
{{/each}}